import 'package:flutter/material.dart';

class TechStackView extends StatelessWidget {
  const TechStackView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const <Widget>[Text("The TechStackView go here")],
    );
  }
}
